1. Sorting String Alphabetically.

Sort String Alphabetically

Once punctuation is removed, convert the string into a list of characters.

Use sorting functions to arrange them alphabetically.

Finally, join them back into a string.

2. Punctuation remove from a string.

Remove Punctuation

Punctuation includes symbols like .,!?;:'"()[]{} etc.

You can filter out only alphanumeric characters (letters and digits) and ignore punctuation.
